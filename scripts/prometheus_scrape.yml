# Add this scrape config snippet to your existing prometheus.yml
#
# scrape_configs:
#   - job_name: pushgateway
#     honor_labels: true
#     static_configs:
#       - targets: ['localhost:9091']    # adjust if pushgateway is on another host
#
# rule_files:
#   - /etc/prometheus/rules/llm_monitor.yml
